# MiOS Unified Knowledge Base
# OpenAI-Compatible Plain Text Format
# Version: 0.1.3 | Generated: 2026-04-28

## SYSTEM IDENTITY

Project: MiOS
Type: bootc-based immutable workstation OS
Base: Fedora Rawhide (OCI/containers)
Version: 0.1.3
Repository: https://github.com/Kabuki94/MiOS-bootstrap
Wiki: https://github.com/Kabuki94/MiOS-bootstrap/wiki
Proprietor: MiOS-DEV
License: Personal Property

## CORE PRINCIPLES

### Documentation Priority
ALWAYS check Wiki FIRST for current information: https://github.com/Kabuki94/MiOS-bootstrap/wiki
Static files (INDEX.md, etc.) are FALLBACK for immutable architecture laws only.

### Immutable Laws (Build-Breaking if Violated)
1. USR-OVER-ETC: Never write static config to /etc/ at build time. Use /usr/lib/<component>.d/
2. NO-MKDIR-IN-VAR: Never mkdir /var/... in scripts. Use tmpfiles.d or StateDirectory=
3. MANAGED-SELINUX: semodule -i in Containerfile RUN layer is the correct method
4. BOUND-IMAGES: Quadlet containers symlinked to /usr/lib/bootc/bound-images.d/
5. BOOT-SHIELDING: Use excludepkgs="shim-*,kernel*" in DNF operations
6. NOVA-CORE-BLACKLIST: On Fedora 44+ (kernel 6.15+), blacklist nouveau AND nova_core
7. BOOTC-CONTAINER-LINT: Must be final Containerfile instruction
8. NO-DNF-UPGRADE-UNCONDITIONAL: Never dnf -y upgrade without package names
9. UNIFIED-AI-REDIRECTS: Use MIOS_AI_* environment variables, target http://localhost:8080/v1

### FHS 3.0 Compliance
Repository uses rootfs-native structure:
- usr/ → System binaries and libraries (immutable)
- etc/ → Configuration templates
- var/ → Mutable state (via tmpfiles.d only)
- home/ → User directories
- automation/ → Build scripts (numbered pipeline)
- specs/ → Documentation and research

## TECHNOLOGY STACK

Base: Fedora Rawhide (bootc/OCI)
Kernel: Latest stable + signed NVIDIA kmods
Init: systemd
Desktop: GNOME (Wayland-only)
Containers: Podman, Quadlet
Sandboxing: Flatpak, Distrobox
Security: SELinux, fapolicyd, composefs, fs-verity
Orchestration: k3s (optional HA mode)
GPU: NVIDIA (vGPU/VFIO), AMD (ROCm), Intel (compute-runtime)

## BUILD PIPELINE

Entry Points:
- Linux/WSL2: just build | just iso | just raw | just all
- Windows: .\mios-build-local.ps1
- One-liner: curl -fsSL https://raw.githubusercontent.com/Kabuki94/MiOS-bootstrap/main/bootstrap.sh | bash

Master Runner: automation/build.sh
Executes all numbered scripts ([0-9][0-9]-*.sh) sequentially
Exception: 08-system-files-overlay.sh called explicitly in Containerfile

Base Image: ghcr.io/ublue-os/ucore-hci:stable-nvidia
Output Image: localhost/mios:latest

## PACKAGE MANAGEMENT

SSOT: usr/share/mios/PACKAGES.md contains ALL packages
Format: Fenced code blocks with category tags (```packages-<category>)
Installation: install_packages <category> from automation/lib/packages.sh
NEVER modify packages outside this system
Edits: Surgical only, never wholesale regeneration

## AI API INTEGRATION

### FOSS-First Design
Vendor-neutral, prioritizes FOSS AI APIs:
- Ollama (local inference)
- llama.cpp (CPU/GPU inference)
- LocalAI (OpenAI-compatible)
- vLLM (GPU-accelerated)
- LangChain / LlamaIndex

### Environment Variables
MIOS_AI_KEY - API key
MIOS_AI_MODEL - Model name (e.g., llama3.1:8b, gpt-4)
MIOS_AI_ENDPOINT - API endpoint (default: http://localhost:8080/v1)
MIOS_AI_TEMPERATURE - Temperature 0.0-1.0 (default: 0.7)

### Function Calling
Available functions (.ai/tools.json):
- mios_update: System updates via bootc
- mios_status: System status and health
- mios_vfio_check: VFIO GPU passthrough readiness
- mios_vfio_toggle: PCIe device binding for VMs
- mios_package_search: Search PACKAGES.md SSOT
- mios_build: Trigger image build

All functions return JSON output.

## KNOWLEDGE SOURCES (Prioritized)

Primary (Load First):
1. INDEX.md (364 lines) - Architecture laws, immutable rules
2. AI-KNOWLEDGE-CONSOLIDATED.md (713 lines) - Current technical knowledge
3. HISTORICAL-KNOWLEDGE-COMPRESSED.md (644 lines) - Historical context
4. AI-AGENT-GUIDE.md (174 lines) - Hard rules, protected files

Secondary (Context as Needed):
- usr/share/mios/PACKAGES.md - Package SSOT
- Containerfile - Build definition
- automation/build.sh - Master build runner
- Justfile - Build automation
- specs/ - Technical documentation

Live (Always Check First):
- Wiki: https://github.com/Kabuki94/MiOS-bootstrap/wiki
- Bootstrap: https://github.com/Kabuki94/MiOS-bootstrap

## MEMORY SYSTEM

Episodic Memory:
- Path: var/lib/mios/memory/journal/v1.jsonl
- Format: JSON Lines (one event per line)
- Interface: JOURNAL.md (human-readable symlink)

Semantic Memory:
- Path: .ai/foundation/memories/
- Format: Markdown files per topic
- Usage: Named .md files for long-term knowledge

Working Memory:
- Path: .ai/foundation/shared-tmp/
- Format: Mixed (JSON, text, temporary data)
- Usage: Cross-agent scratchpad

## PROTECTED FILES (DO NOT MODIFY)

- VERSION and CHANGELOG.md - Managed by push-to-github.ps1 only
- usr/share/mios/PACKAGES.md - Surgical edits only
- .github/workflows/ - CI/CD definitions
- specs/memory/** - AI semantic memory store

## DELIVERABLE CONTRACT

1. Complete files only - No patches, no diffs, no "paste this into X"
2. One push script - push-to-github.ps1 (clone → copy → commit → push)
3. Never git init - Always clone existing repo
4. No unsupervised pushes - Require human review before push

## OPERATIONAL PATTERNS

Context Retrieval:
1. Query .ai/context.json to find relevant manifest
2. Read manifest to locate specific files
3. Use surgical edits for large files (PACKAGES.md)

Validation:
- just lint - bootc container lint
- just test - System smoke tests
- ./evals/smoke-test.sh - Image validation

Secret Handling:
- NEVER output or commit secrets
- Use placeholders like INJ_PASSWORD in templates
- Redact sensitive data in logs

## RESPONSE FORMAT

When responding as AI agent:
1. Check Wiki first for current information
2. Cite sources with file paths and line numbers (file.sh:42)
3. Use markdown links for file references: [file.sh:42](file.sh#L42)
4. Validate immutable laws before suggesting changes
5. Propose verification steps after modifications
6. Never commit secrets or sensitive data

## QUICK START FOR AI AGENTS

# Load primary context
cat INDEX.md AI-KNOWLEDGE-CONSOLIDATED.md HISTORICAL-KNOWLEDGE-COMPRESSED.md

# Check Wiki for latest
curl https://github.com/Kabuki94/MiOS-bootstrap/wiki/Home

# Load knowledge graph
cat artifacts/ai-rag/mios-knowledge-graph.json

# Review RAG manifest
cat .ai/rag-config.yaml

# Bootstrap AI environment
./automation/ai-bootstrap.sh

## KEY STATISTICS

Version: 0.1.3
Total Knowledge Lines: 1,895
Compressed Artifacts: 509 KB (99.95% compression from 928 MB)
Build Scripts: 40+ numbered automation scripts
FHS Compliance: 100%
AI Files: 4 primary, 15+ supporting
Supported GPUs: NVIDIA, AMD, Intel
Deployment Surfaces: Bare metal, WSL2, Hyper-V, KVM
Security Layers: SELinux, fapolicyd, composefs, fs-verity

## COMMON TASKS

Build OCI Image:
just build

Generate ISO Installer:
just iso

Generate RAW Disk Image:
just raw

Full Pipeline:
just all

Run Tests:
just lint
just test
./evals/smoke-test.sh localhost/mios:latest

Update System:
sudo bootc upgrade

Check Status:
bootc status --json

## DEBUGGING

Build Logs: /usr/lib/mios/logs/build.log
System Logs: journalctl -u <service>
Container Logs: podman logs <container>
Greenboot Logs: journalctl -u greenboot-status

## VERSION HISTORY

MiOS-1: Original Fedora bootc + akmod drivers
MiOS-2/v0.1.x: ucore-hci base + pre-signed NVIDIA (current)
MiOS-NXT: Future (Project Hummingbird, SBOM, minimal variants)

## EMBEDDING STRATEGY

Model: all-MiniLM-L6-v2 (HuggingFace)
Dimensions: 384
Chunk Size: 512 tokens
Chunk Overlap: 50 tokens
Distance Metric: Cosine similarity
Top K: 5
Score Threshold: 0.7

## METADATA

Generated: 2026-04-28T03:49:00Z
Schema Version: 1.0.0
Format: Plain text for maximum API compatibility
Encoding: UTF-8
License: Personal Property - MiOS-DEV
