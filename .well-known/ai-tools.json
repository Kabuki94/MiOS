{
  "version": "1.1.0",
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "mios_update",
        "description": "Check for and apply MiOS system updates via bootc. Can return current image status or stage new updates.",
        "parameters": {
          "type": "object",
          "properties": {
            "json": {
              "type": "boolean",
              "description": "Return output in machine-readable JSON format. Mandatory for AI API calls.",
              "default": true
            },
            "check_only": {
              "type": "boolean",
              "description": "Only check for updates without staging or applying them.",
              "default": false
            }
          },
          "required": ["json"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "mios_status",
        "description": "Get detailed system and service status information for the MiOS node.",
        "parameters": {
          "type": "object",
          "properties": {
            "json": {
              "type": "boolean",
              "description": "Return output in machine-readable JSON format.",
              "default": true
            }
          }
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "mios_vfio_check",
        "description": "Check system readiness for VFIO GPU passthrough, including IOMMU status and module availability.",
        "parameters": {
          "type": "object",
          "properties": {
            "json": {
              "type": "boolean",
              "description": "Return output in machine-readable JSON format.",
              "default": true
            }
          }
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "mios_vfio_toggle",
        "description": "Dynamically bind or unbind a PCIe device to the vfio-pci driver for VM passthrough.",
        "parameters": {
          "type": "object",
          "properties": {
            "json": {
              "type": "boolean",
              "description": "Return output in machine-readable JSON format.",
              "default": true
            },
            "pci_slot": {
              "type": "string",
              "description": "The PCI address of the device (e.g., 0000:01:00.0)."
            },
            "action": {
              "type": "string",
              "enum": ["bind", "unbind"],
              "description": "The action to perform."
            }
          },
          "required": ["pci_slot", "action"]
        }
      }
    }
  ]
}
