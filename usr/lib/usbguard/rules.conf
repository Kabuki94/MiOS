# MiOS Default USBGuard Rules
# Allow all devices currently connected on boot (so keyboards/mice work)
# New devices inserted after boot will be blocked until explicitly authorized.
# Use `usbguard list-devices` and `usbguard allow-device <id>` to whitelist.

allow id *:* hash "*" name "*" serial "*" via-port "*" with-interface "*"
