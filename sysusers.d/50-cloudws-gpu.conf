# CloudWS-bootc v2.1.5 - pin Fedora static GIDs for container GPU passthrough.
# setup package ships these as static but render is still listed as
# "soft-static" upstream — pin explicitly to avoid drift.
# Format: Type Name ID GECOS HomeDir Shell
g video  39  "Video capture and GPU modesetting"
g render 105 "Direct Rendering Infrastructure and GPU compute"
