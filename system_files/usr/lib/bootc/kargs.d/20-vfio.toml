# VFIO / IOMMU for GPU passthrough. Safe to leave on even when no VM is running.
kargs = [
  "amd_iommu=on",
  "iommu=pt",
  "rd.driver.pre=vfio_pci",
  "kvm.ignore_msrs=1",
  "kvm.report_ignored_msrs=0",
]
